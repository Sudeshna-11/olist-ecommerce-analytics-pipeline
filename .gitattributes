# Power BI reports are zip archives — never let Git normalize line endings,
# it would corrupt the binary.
*.pbix binary
*.pbit binary

# Image assets
*.png binary
*.jpg binary
*.jpeg binary

# Shell scripts must stay LF — they run inside Linux containers (a CRLF
# entrypoint.sh breaks the shebang on a Windows checkout).
*.sh text eol=lf
